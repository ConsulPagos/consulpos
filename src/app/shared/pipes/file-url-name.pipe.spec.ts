import { FileUrlNamePipe } from './file-url-name.pipe';

describe('FileUrlNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FileUrlNamePipe();
    expect(pipe).toBeTruthy();
  });
});
