import { BlockcpDirective } from './blockcp.directive';

describe('BlockcpDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockcpDirective();
    expect(directive).toBeTruthy();
  });
});
